[project]
name = "axion-lab-ws"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["axion-lab"]

[tool.uv.sources]
axion-lab = { workspace = true }

[tool.uv.workspace]
members = ["axion_lab/"]
exclude = ["axion_lab/tests"]
