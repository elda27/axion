[project]
name = "axion-ws"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["axion"]

[tool.uv.sources]
axion = { workspace = true }

[tool.uv.workspace]
members = ["axion/"]
exclude = ["axion/tests"]
