# Artifact-first Experiment Evaluation System

This system evaluates experiments by treating generated artifacts
(JSON, Excel, text, images, logs, etc.) as the primary truth.

Metrics are not fixed up-front.
They are discovered, revised, and re-applied after observing real outputs.

The goal is not automation of model selection,
but enabling humans to derive insight and invent better metrics.
