# Query Use Cases

The system must support bulk queries to avoid N+1 patterns.

Key queries:
- fetch artifacts by run_id
- fetch final outputs for many runs
- fetch all outputs for a case across runs
- fetch two artifacts for comparison
- fetch latest output by user-defined key
