name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

env:
  PYTHON_VERSION: '3.12'
  NODE_VERSION: '22'
  PNPM_VERSION: '10'

jobs:
  test:
    name: Run Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          version: "latest"

      - name: Install dependencies
        run: |
          uv sync --frozen --all-extras

      - name: Run linting
        run: |
          uv run ruff check src/

      - name: Run type checking
        run: |
          uv run ty check src/

      - name: Run tests
        run: |
          uv run pytest tests/ -v --cov=src

  build:
    name: Build Applications
    runs-on: ubuntu-latest
    needs: test
    outputs:
      backend_artifact: ${{ steps.upload-backend.outputs.artifact-id }}
      frontend_artifact: ${{ steps.upload-frontend.outputs.artifact-id }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          version: "latest"

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Install frontend dependencies
        run: |
          pnpm install --frozen-lockfile

      - name: Build Backend
        run: |
          mkdir -p dist/backend
          cp -r src/axion src/axion_server src/axion_alembic dist/backend/
          cp pyproject.toml uv.lock alembic.ini dist/backend/
          cp -r deploy dist/backend/
          cd dist && tar -czf axion-backend.tar.gz backend/

      - name: Build Frontend
        working-directory: src/axion_ui
        run: |
          pnpm build

      - name: Build Storybook
        if: github.ref == 'refs/heads/main'
        working-directory: src/axion_ui
        run: |
          pnpm build-storybook

      - name: Package Frontend
        run: |
          mkdir -p dist
          cd src/axion_ui/dist && tar -czf ../../../dist/axion-frontend.tar.gz .

      - name: Package Storybook
        if: github.ref == 'refs/heads/develop'
        run: |
          mkdir -p dist
          cd src/axion_ui/storybook-static && tar -czf ../../../dist/axion-storybook.tar.gz .

      - name: Upload Backend Artifact
        id: upload-backend
        uses: actions/upload-artifact@v4
        with:
          name: axion-backend
          path: dist/axion-backend.tar.gz
          retention-days: 7

      - name: Upload Frontend Artifact
        id: upload-frontend
        uses: actions/upload-artifact@v4
        with:
          name: axion-frontend
          path: dist/axion-frontend.tar.gz
          retention-days: 7

      - name: Upload Storybook Artifact
        if: github.ref == 'refs/heads/develop'
        id: upload-storybook
        uses: actions/upload-artifact@v4
        with:
          name: axion-storybook
          path: dist/axion-storybook.tar.gz
          retention-days: 7

  # Trigger deployment workflow on success (main/develop only)
  trigger-deploy:
    name: Trigger Deployment
    runs-on: ubuntu-latest
    needs: build
    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop')
    steps:
      - name: Trigger deploy workflow
        uses: actions/github-script@v7
        with:
          script: |
            const environment = context.ref === 'refs/heads/main' ? 'production' : 'staging';
            const deployStorybook = context.ref === 'refs/heads/develop';
            
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: 'deploy-gcp.yml',
              ref: context.ref,
              inputs: {
                environment: environment,
                deploy_storybook: String(deployStorybook),
                ci_run_id: String(context.runId)
              }
            });
            
            console.log(`Triggered deployment to ${environment}`);
